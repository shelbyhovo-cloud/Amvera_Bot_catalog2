# 🚀 Деплой на Amvera

Пошаговая инструкция по деплою Telegram Mini App на Amvera.

## 📋 Подготовка

### 1. Регистрация на Amvera
1. Перейди на https://amvera.ru
2. Зарегистрируйся (можно через GitHub)
3. Подтверди email

### 2. Создай репозиторий на GitHub
1. Перейди на https://github.com
2. Нажми "New repository"
3. Название: `telegram-mini-app` (или любое другое)
4. Выбери "Public" или "Private"
5. Нажми "Create repository"

---

## 📤 Загрузка кода на GitHub

### Вариант 1: Через Git (рекомендуется)

```bash
cd "c:\Users\Hovo\OneDrive\Desktop\minin app"

# Инициализируй Git репозиторий
git init

# Добавь все файлы
git add .

# Создай первый коммит
git commit -m "Initial commit - Telegram Mini App"

# Подключи GitHub репозиторий (замени URL на свой!)
git remote add origin https://github.com/твой-username/telegram-mini-app.git

# Отправь код
git branch -M main
git push -u origin main
```

### Вариант 2: Через веб-интерфейс GitHub

1. Открой свой репозиторий на GitHub
2. Нажми "uploading an existing file"
3. Перетащи все файлы из папки `minin app`
4. Нажми "Commit changes"

---

## 🔧 Настройка на Amvera

### 1. Создай проект

1. Войди в панель Amvera: https://amvera.ru/dashboard
2. Нажми **"Создать проект"**
3. Выбери **"Из GitHub"**
4. Подключи свой GitHub аккаунт (если еще не подключен)
5. Выбери репозиторий `telegram-mini-app`

### 2. Настрой проект

**Основные настройки:**
- **Язык**: Python
- **Версия Python**: 3.12
- **Команда запуска**: Оставь по умолчанию (используется Procfile)
- **Порт**: 8080

**Переменные окружения (опционально):**
Если хочешь скрыть токен бота, добавь переменную:
- `BOT_TOKEN` = `твой_токен_бота`

Затем в `mini_app.py` измени строку 195:
```python
BOT_TOKEN = os.getenv("BOT_TOKEN", "8529662300:AAHnb8e8Qh93INgnC_x3rkDc1QC20c3ulFM")
```

### 3. Настрой MODE для Amvera

В `mini_app.py` найди строку 202 и измени:

```python
MODE = "manual"  # Для Amvera используем ручной режим
```

И на строке 205 после деплоя вставишь URL от Amvera:
```python
MANUAL_WEBAPP_URL = "https://твой-проект.amvera.io"
```

**ИЛИ** лучше - укажи переменную окружения:
```python
MANUAL_WEBAPP_URL = os.getenv("WEBAPP_URL", "https://твой-проект.amvera.io")
```

### 4. Запусти деплой

1. Нажми **"Развернуть"**
2. Amvera автоматически:
   - Установит зависимости из `requirements.txt`
   - Запустит бота через `Procfile`
   - Даст HTTPS URL

### 5. Получи URL

После деплоя получишь URL вида:
```
https://твой-проект-abc123.amvera.io
```

### 6. Обнови настройки бота

1. Скопируй URL от Amvera
2. В панели Amvera или локально в файле `mini_app.py` обнови:
   ```python
   MODE = "manual"
   MANUAL_WEBAPP_URL = "https://твой-проект-abc123.amvera.io"
   ```
3. Если меняешь локально - сделай commit и push:
   ```bash
   git add mini_app.py
   git commit -m "Update WEBAPP_URL for Amvera"
   git push
   ```
4. Amvera автоматически задеплоит новую версию

---

## ✅ Проверка

1. Открой URL от Amvera в браузере - должен показаться каталог товаров
2. Открой Telegram → напиши боту `/start`
3. Нажми **"🛍 Открыть магазин"**
4. Должен открыться Mini App с каталогом!

---

## 🔄 Обновление каталога

### Способ 1: Через ZIP архив (прямо в Telegram)
1. В `parser_gui.py` создай новый каталог
2. Нажми **"Заархивировать"**
3. Отправь ZIP архив боту в Telegram
4. Бот автоматически обновит каталог

### Способ 2: Через Git
1. Обнови `products_links.xlsx` и папку `images/`
2. Сделай commit и push:
   ```bash
   git add products_links.xlsx images/
   git commit -m "Update catalog"
   git push
   ```
3. Amvera автоматически задеплоит

---

## 📊 Логи и мониторинг

**Просмотр логов:**
1. Панель Amvera → Твой проект
2. Вкладка **"Логи"**
3. Смотри вывод бота в реальном времени

**Перезапуск:**
- Кнопка **"Перезапустить"** в панели

---

## 💰 Тарифы

**Бесплатный тариф:**
- ✅ 1 проект
- ✅ HTTPS автоматически
- ✅ Достаточно для тестирования

**Платный тариф (если нужно больше):**
- Несколько проектов
- Больше ресурсов
- ~300₽/месяц

---

## 🛠️ Возможные проблемы

### Бот не запускается
**Проверь логи:**
- Панель Amvera → Логи
- Ищи ошибки

**Частые причины:**
- Неправильный `BOT_TOKEN`
- Не установлен `MODE = "manual"`
- Неправильный `MANUAL_WEBAPP_URL`

### Mini App не открывается
**Проверь:**
1. URL в `MANUAL_WEBAPP_URL` правильный
2. URL начинается с `https://` (не `http://`)
3. Бот запущен (смотри логи)

---

## 🎉 Готово!

Теперь твой бот работает 24/7 с автоматическим HTTPS!

**Плюсы Amvera:**
- ✅ На русском
- ✅ Простой интерфейс
- ✅ Автоматический деплой
- ✅ Бесплатный тариф

**Вопросы?**
Пиши в поддержку Amvera или проверь документацию: https://docs.amvera.ru
