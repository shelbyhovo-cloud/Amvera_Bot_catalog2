# 🛍 Telegram Mini App - Магазин

Интернет-магазин для Telegram группы с веб-каталогом товаров и корзиной.

## 📋 Что это?

- **Telegram бот** с командами `/start` и `/shop`
- **Веб-интерфейс** (Mini App) — каталог товаров с корзиной
- **Отправка заказов** прямо в чат или группу

## 🚀 Быстрый старт

### Просто запусти!

```bash
python mini_app.py
```

**Автоматически произойдёт:**
1. ✅ Установка зависимостей (aiogram, aiohttp, openpyxl)
2. ✅ Освобождение порта 8080
3. ✅ Запуск Serveo туннеля и получение HTTPS URL (бесплатно, без регистрации)
4. ✅ Загрузка каталога из Excel
5. ✅ Запуск веб-сервера
6. ✅ Запуск Telegram бота

**Всё готово!** Напиши боту `/start` в Telegram.

> 💡 **Serveo** - бесплатный сервис туннелирования, не требует регистрации!

---

## 📊 Управление товарами через Excel

Можешь управлять каталогом через Excel файл с автоматическим парсингом цен!

### Создай шаблон Excel

```bash
python update_products.py
```

Откроется файл `products_links.xlsx`. Вставь туда ссылки на товары и запусти снова:

```bash
python update_products.py  # Парсит товары
python mini_app.py          # Запускает бота с новыми товарами
```

📖 **Подробная инструкция:** [EXCEL_GUIDE.md](EXCEL_GUIDE.md)

---

## 🧪 Тестирование

1. Найди своего бота в Telegram
2. Напиши `/start`
3. Нажми кнопку **"🛍 Открыть магазин"**
4. Выбери товары, добавь в корзину
5. Нажми **"Оформить заказ"**

Заказ придёт в чат с ботом!

## 🔧 Настройка

### Изменить каталог товаров

Открой [mini_app.py](mini_app.py) и отредактируй список `PRODUCTS`:

```python
PRODUCTS = [
    {
        "id": 1,
        "name": "Название товара",
        "description": "Описание",
        "price": 1000,
        "image": "🎁",  # Эмодзи или URL картинки
    },
    # Добавь свои товары...
]
```

### Отправка заказов в группу

Чтобы заказы приходили в группу/канал:

1. Добавь бота в группу и сделай его администратором
2. Узнай ID группы (используй бота `@userinfobot` или `@getidsbot`)
3. В [mini_app.py](mini_app.py:145) раскомментируй строки:

```python
# Отправить заказ в группу
CHANNEL_ID = -1001234567890  # Вставь ID твоей группы
await bot.send_message(CHANNEL_ID, order_text, parse_mode="HTML")
```

## 📁 Структура проекта

```
minin app/
├── mini_app.py              # 🤖 Основной файл (бот + сервер + HTML)
├── parser_gui.py            # 🕷️ GUI для парсинга товаров
├── README.md                # 📖 Основная инструкция
├── products_links.xlsx      # 📄 Excel с товарами
├── images/                  # 📁 Папка с фотографиями товаров
├── kill_port.py            # 🔧 Утилита освобождения порта
└── check_excel.py          # 📊 Проверка Excel файла
```

**Все зависимости устанавливаются автоматически!**

## 🛠 Возможные проблемы

### ❌ "Telegram.WebApp is not defined"

**Решение:** Убедись, что WEBAPP_URL указывает на правильный HTTPS URL от Serveo.

### ❌ Serveo не подключается

**Решение:**
1. Проверь что SSH клиент установлен (Windows 10/11: Параметры → Дополнительные компоненты → OpenSSH Client)
2. Попробуй ручной режим: `ssh -R 80:localhost:8080 serveo.net`
3. Используй альтернативу: LocalTunnel (`npx localtunnel --port 8080`)

### ❌ Порт 8080 занят

**Решение:** Бот автоматически освобождает порт. Если не помогло:
```bash
python kill_port.py 8080
```

### ❌ Бот не отвечает

**Решение:** Проверь, что:
1. BOT_TOKEN правильный
2. Бот запущен (`python mini_app.py`)
3. Нет других запущенных ботов с этим токеном

### ❌ Mini App не открывается

**Решение:**
1. Проверь, что Serveo туннель работает
2. WEBAPP_URL начинается с `https://` (не `http://`)
3. Попробуй перезапустить бота

## 📦 Деплой в продакшн

Для постоянной работы нужен сервер с HTTPS:

- **VPS/VDS** (DigitalOcean, Hetzner, etc.)
- **Бесплатные варианты**: Railway, Render, Fly.io
- **SSL сертификат**: Let's Encrypt (бесплатно)

## 📝 Лицензия

Свободное использование. Удачи! 🚀
